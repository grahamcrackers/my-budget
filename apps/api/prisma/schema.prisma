generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "postgresql"
    url      = env("DATABASE_URL")
}

model Budget {
    id         String    @id @default(uuid())
    name       String
    firstMonth DateTime  @default(now())
    lastMonth  DateTime?  
    created    DateTime  @default(now())
    createdBy  String    @db.Uuid
    updated    DateTime  @updatedAt
    updatedBy  String    @db.Uuid
    Account    Account[]

    @@map("budgets")
}

model Account {
    id        String   @id @default(uuid())
    name      String
    balance   Int      @default(0)
    type      String?
    note      String?
    created   DateTime @default(now())
    createdBy String   @db.Uuid
    updated   DateTime @updatedAt
    updatedBy String   @db.Uuid
    budget    Budget   @relation(fields: [budgetId], references: [id])
    budgetId  String

    @@map("accounts")
}


// LKP Tables
// DateFormat
// CurrencyFormat
